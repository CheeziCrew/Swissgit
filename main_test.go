package main

import "testing"

// TestBuild is a build verification test — if this compiles,
// the main package and all its imports are valid.
func TestBuild(t *testing.T) {
	// This test intentionally does nothing beyond verifying
	// that the main package compiles successfully.
}
